type Category {
    id: ID!
    name: String!
    slug: String!
    description: String
    parent_id: ID
    children: [Category!]!
}

type Query {
    categories: [Category!]! @field(resolver: "App\\GraphQL\\Queries\\CategoryQuery@fetchCategories")
}
